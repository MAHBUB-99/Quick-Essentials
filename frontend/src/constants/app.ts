/** Static, app-wide constants. Keep values that describe the brand/config here. */

export const APP_NAME = 'FarmFresh';
export const APP_TAGLINE = 'Local Farmer Booking';
export const APP_DESCRIPTION =
  'FarmFresh connects you directly with local farmers for fresh, organic produce delivered to your door.';

/** Bangladeshi Taka — the currency used throughout the source templates. */
export const CURRENCY_SYMBOL = '৳';

export const SUPPORT_EMAIL = 'support@farmfresh.example';

/** Simulated network latency (ms) for the mock service layer. */
export const MOCK_LATENCY_MS = 400;

/** Default page sizes. */
export const PRODUCTS_PER_PAGE = 9;
export const REVIEWS_PAGE_SIZE = 5;
